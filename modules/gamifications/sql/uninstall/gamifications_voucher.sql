CREATE TABLE IF NOT EXISTS `PREFIX_gamifications_voucher` (
    `id_cart_rule` INT UNSIGNED NOT NULL,
    `id_gamifications_activity_history` INT UNSIGNED NOT NULL,
    PRIMARY KEY (`id_cart_rule`)
)ENGINE=ENGINE_TYPE;
